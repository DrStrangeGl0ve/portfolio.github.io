We can now use different names and tags to plot points. Can still add regional URL checks if we want, but for now NA is fine.
Now the points are overlaid on the map of summoner's rift with timestamps. Need to generate a different pic for each of the requested games.